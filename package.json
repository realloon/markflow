{
  "name": "@realloon/markflow",
  "description": "An incremental Markdown parser for streaming output.",
  "publishConfig": {
    "access": "public"
  },
  "version": "0.2.0",
  "type": "module",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "bun test",
    "benchmark": "bun run benchmark/stream.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
